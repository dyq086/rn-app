@import '../common/index.wxss';.van-dialog {
  top: 45% !important;
  overflow: hidden;
  width: 640px;
  width: var(--dialog-width, 640px);
  font-size: 32px;
  font-size: var(--dialog-font-size, 32px);
  border-radius: 32px;
  border-radius: var(--dialog-border-radius, 32px);
  background-color: #fff;
  background-color: var(--dialog-background-color, #fff);
}
@media (max-width: 321px) {
  .van-dialog {
    width: 90%;
    width: var(--dialog-small-screen-width, 90%);
  }
}
.van-dialog__header {
  text-align: center;
  padding-top: 48px;
  padding-top: var(--dialog-header-padding-top, 48px);
  font-weight: 500;
  font-weight: var(--dialog-header-font-weight, 500);
  line-height: 48px;
  line-height: var(--dialog-header-line-height, 48px);
}
.van-dialog__header--isolated {
  padding: 48px 0;
  padding: var(--dialog-header-isolated-padding, 48px 0);
}
.van-dialog__message {
  overflow-y: auto;
  text-align: center;
  -webkit-overflow-scrolling: touch;
  font-size: 28px;
  font-size: var(--dialog-message-font-size, 28px);
  line-height: 40px;
  line-height: var(--dialog-message-line-height, 40px);
  max-height: 60vh;
  max-height: var(--dialog-message-max-height, 60vh);
  padding: 48px;
  padding: var(--dialog-message-padding, 48px);
}
.van-dialog__message-text {
  word-wrap: break-word;
}
.van-dialog__message--has-title {
  padding-top: 24px;
  padding-top: var(--dialog-has-title-message-padding-top, 24px);
  color: #646566;
  color: var(--dialog-has-title-message-text-color, #646566);
}
.van-dialog__message--left {
  text-align: left;
}
.van-dialog__message--right {
  text-align: right;
}
.van-dialog__footer {
  display: flex;
}
.van-dialog__button {
  flex: 1;
}
.van-dialog__confirm,
.van-dialog__cancel {
  border: 0 !important;
}
.van-dialog-bounce-enter {
  transform: translate3d(-50%, -50%, 0) scale(0.7);
  opacity: 0;
}
.van-dialog-bounce-leave-active {
  transform: translate3d(-50%, -50%, 0) scale(0.9);
  opacity: 0;
}
